Класс называется *абстрактным*, когда его единственная цель - определение интерфейса. Класс не реализует ни один метод, а предоставляет всё это для потомков.

Все остальные классы называются *конкретными*.

[[Класс]]